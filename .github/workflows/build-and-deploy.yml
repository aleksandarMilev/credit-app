name: Build (develop / master)

on:
  push:
    branches: [develop, master]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Placeholder
        run: echo "Repository is empty — nothing to build yet."